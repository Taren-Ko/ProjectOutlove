using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterSwitch : MonoBehaviour {


	public GameObject playerChar;

	

	// private int selectedCharacter = 1;
	// private string characterName;
	// private string currCharacterName;

	// private Vector3 currTransform;

	// public Transform C1;
	// public Transform C2;


	// // Use this for initialization
	// void Start () {
		
	// }
	
	// // Update is called once per frame
	// void Update () {
	// 	if(Input.GetKeyDown(KeyCode.X)){
	// 		if(selectedCharacter == 1){
				
				
	// 			Instantiate(C2, currTransform, Quaternion.identity);
	// 			selectedCharacter++;

    //             // currCharacterName = "C" + (i - 1).ToString();
    //             // print(currCharacterName);
    //             // characterName = "C" + i.ToString();
    //             // print(characterName);
    //             // currTransform = GameObject.Find(currCharacterName).transform.position;
    //             // GameObject.Find(currCharacterName).SetActive(false);
	// 			// GameObject.Find(characterName).SetActive(true);
	// 			// GameObject.Find(characterName).transform.position = currTransform;
	// 		}
	// 		else{
	// 			Instantiate(C1, currTransform, Quaternion.identity);
	// 			selectedCharacter--;
	// 		}
			
	// 	}
	// }
}
